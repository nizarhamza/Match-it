{
  "name": "match-it",
  "version": "1.0.0",
  "private": true,
  "description": "Everyone writes a word each round until the whole room lands on the same one.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/game-core.test.mjs",
    "test:room": "node --test --test-concurrency=1 test/room.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
